<IfModule mod_rewrite.c>
    RewriteEngine On

    # Always serve the Laravel public directory as web root.
    RewriteRule ^$ public/ [L]
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
